import numpy as np
n,m,r = map(int,input().split( ))
np1 = np.empty((n,m))
np2 = np.empty((m,r))
count = 0
while count < n:
    np1[count] = input().split( )
    count += 1
count = 0 
while count < m:
    np2[count] = input().split( )
    count += 1

N = 0
while N < n:
    R = 0
    while R < r:
        num = 0
        M = 0
        while M < m:
            num += np1[N][M]*np2[M][R] 
            M += 1
        print(int(num))
        R += 1
    N += 1
    
